# Install pg Package

Better Auth ko PostgreSQL connection ke liye `pg` package chahiye.

Yeh command run karein:

```bash
cd frontend
pnpm add pg
pnpm add -D @types/pg
```

Phir server restart karein.

